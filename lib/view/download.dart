import 'package:flutter/material.dart';

class DownloadOptionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color.fromARGB(255, 18, 84, 218),
        title: Text('Download'),
      ),
      body: Center(
        child: Text('Empty'),
      ),
    );
  }
}
